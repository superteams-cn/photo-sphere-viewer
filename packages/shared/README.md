# 共享内部包

`@photo-sphere-viewer/shared` 汇集多个包复用的内部工具，例如视频适配器基类、视频创建工具和色键材质。

此包主要供本仓库内部使用，不建议在业务项目中直接依赖。公共 API 请优先使用 `@photo-sphere-viewer/core` 以及各插件、适配器包。

## 开发说明

共享代码应保持小而稳定；新增内容前，请确认至少有两个包需要复用同一能力。

## 许可证

MIT
